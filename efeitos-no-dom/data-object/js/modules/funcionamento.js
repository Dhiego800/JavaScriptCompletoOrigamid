{/*new Date()
  O construtor Date cria um objeto contento valores como 
  mês, dia, ano e MediaSource. A data é baseada no rélogio interno
  do computador.

  const agora = new Date() Semana Mês Ano HH:MM:SS GTM

  agora.getDate() Dia
  agora.getDay() Dia da semana ex: 5 = fri
  agora.getMonth() Número dia mês
  agora.getFullYear() Ano
  agora.getHours() Hora
  agora.getMinutes() Minutos
  agora.getTime() ms desde 1970
  agora.getUTCHours() -3 Brasília 

  const agora = new Date();
  const natal = new Date('Dec 24 2020 23:59');
  
  function convetDias(time){
    return time / (24 * 60 * 60 * 1000);
  };
  
  const diasAgora = convetDias(agora);
  const diasPromocao = convetDias(natal);
  const faltamNatal = diasAgora - diasPromocao;
*/}



export default function initFuncionamento(){

};