console.log(typeof Array.from !== "undefined");

if(typeof Array.from !== "undefined") {
    console.log('existe');
} else {
    console.log('Não Existe');
}

/* O que é uma API ? 
    Appliccation Programming Interdace, é uma interface de software criada para interação
    com outros softwares. Ou seja, toda interação que fazemos com o browser utilizando Objetos, Métodos e
    Propriedades, estamos na verdade interagindo com a API do browser.
*/